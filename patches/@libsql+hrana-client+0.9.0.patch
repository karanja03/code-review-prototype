diff --git a/node_modules/@libsql/hrana-client/lib-cjs/http/stream.js b/node_modules/@libsql/hrana-client/lib-cjs/http/stream.js
index 940a9e5..9ab0c64 100644
--- a/node_modules/@libsql/hrana-client/lib-cjs/http/stream.js
+++ b/node_modules/@libsql/hrana-client/lib-cjs/http/stream.js
@@ -347,7 +347,8 @@ async function decodePipelineResponse(resp, encoding) {
         const respData = await resp.arrayBuffer();
         return (0, index_js_1.readProtobufMessage)(new Uint8Array(respData), protobuf_decode_js_1.PipelineRespBody);
     }
-    await resp.body?.cancel();
+    if (typeof resp.body?.cancel === "function")
+        await resp.body.cancel();
     throw (0, util_js_1.impossible)(encoding, "Impossible encoding");
 }
 async function errorFromResponse(resp) {
@@ -367,6 +368,7 @@ async function errorFromResponse(resp) {
         }
         return new errors_js_1.HttpServerError(message, resp.status);
     }
-    await resp.body?.cancel();
+    if (typeof resp.body?.cancel === "function")
+        await resp.body.cancel();
     return new errors_js_1.HttpServerError(message, resp.status);
 }
diff --git a/node_modules/@libsql/hrana-client/lib-esm/http/stream.js b/node_modules/@libsql/hrana-client/lib-esm/http/stream.js
index 117fd8f..6138d6d 100644
--- a/node_modules/@libsql/hrana-client/lib-esm/http/stream.js
+++ b/node_modules/@libsql/hrana-client/lib-esm/http/stream.js
@@ -343,7 +343,8 @@ async function decodePipelineResponse(resp, encoding) {
         const respData = await resp.arrayBuffer();
         return readProtobufMessage(new Uint8Array(respData), protobuf_PipelineRespBody);
     }
-    await resp.body?.cancel();
+    if (typeof resp.body?.cancel === "function")
+        await resp.body.cancel();
     throw impossible(encoding, "Impossible encoding");
 }
 async function errorFromResponse(resp) {
@@ -363,6 +364,7 @@ async function errorFromResponse(resp) {
         }
         return new HttpServerError(message, resp.status);
     }
-    await resp.body?.cancel();
+    if (typeof resp.body?.cancel === "function")
+        await resp.body.cancel();
     return new HttpServerError(message, resp.status);
 }
